{
  "name": "ppt",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "parcel": "^2.15.4"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "dev": "parcel serve src/index.html",
    "build": "parcel build src/index.html --dist-dir build --public-url /PPT_offline",
    "deploy": "gh-pages -d build"
  }
}
